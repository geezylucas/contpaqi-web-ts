export type PostType = {
  title: string;
  description: string;
  image: string;
  imageText: string;
};

export type FeaturedPostType = {
  title: string;
  date: string;
  description: string;
  image: string;
  imageText: string;
};
